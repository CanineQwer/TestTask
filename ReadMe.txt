Задачу можно решить многими способами.
Самый важный нюанс это поиск простых чисел.
Рассмотрим 2 основных варианта:
1.
Реализовать самый оптимальный алгоритм "Решето Эратосфена"(30 строк копипасты из интернета).
И пустить по итоговому массиву 2 читающих синхронизированных потока.
2.
Пустить 2 потока которые будут и проверять числа на "простоту" и записывать их в файл.
Там есть небольшой нюанс с синхронизацией.
Нельзя чтобы один из потоков вырывался вперед, ибо может потеряться второе условие ТЗ, а именно возрастающий порядок.
Поэтому надо их синхронизировать.
//
Я не понимаю что-бы вы хотели видеть.
1.
В пользу первого варианта говорит "миллион", ибо второй вариант уж больно медленный.
2.
В пользу второго варианта говорит то что первый вариант слишком простой(копипаста 30 строк кода из сети и 2 потока читающих массив).
А так-же колличество потоков(ведь простые числа не могут стоять друг за другом(исключение 2 и 3), но могут идти через одно число и поэтому 2 потока на проверку и запись это оптимальное колличество для адекватной синхронизации в этом варианте).
//
Я предположил что вы хотите видеть второй способ.